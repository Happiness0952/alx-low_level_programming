#include<stdio.h>
/**
 * main - Entry point
 *
 * Description: 'using puts to print a string'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Mytxt[] = "with proper grammar, but the outcome is a piece of art,"

	printf("%s", Mytxt);
	return (0);
}
